{
  "name": "quarkus-angular-ui",
  "version": "1.0.0",
  "description": "How to use create-react-app with a custom Node API on Heroku",
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "start": "node server",
    "build": "npm install -g @angular/cli && cd angular-ui/ && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "angular-ui/node_modules"
  ],
  "dependencies": {
    "express": "^4.16.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mars/heroku-cra-node.git"
  },
  "keywords": [
    "node",
    "heroku",
    "angular"
  ],
  "license": "MIT",
  "devDependencies": {}
}
